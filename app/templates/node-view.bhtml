<div
  @class node
  <span
    @ng-bind c.model.children.length > 0 ? (c.model.open ? '-' : '+') : ' '
    @ng-click c.click()
  &nbsp;
  <span
    @class node-name
    @ng-bind c.model.name
    @ng-click c.click()
  <ul
    @ng-show c.model.open
